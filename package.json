{
  "name": "magickaldiary",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install": "concurrently \"npm install --prefix backend\" \"npm install --prefix frontend\" \"npm install\"",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
